import styles from '@/app/page.module.scss';

export default async function Page() {
    return (
        <main className={styles.main}>
            <p>This is a sample blog created with NextJS + Strapi by @kristijorgji</p>
        </main>
    );
}
